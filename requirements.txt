# Runtime dependencies for the Auto Manager service.
# Range pins, not exact pins: Render's build resolves the latest compatible
# version, and the test suite is what protects against a bad upgrade.
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
asyncpg>=0.29,<1.0
telethon>=1.36,<2.0
