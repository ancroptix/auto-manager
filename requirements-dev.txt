# Development / CI only. Never installed by Render's build command.
-r requirements.txt
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
pglast>=6
# pgserver bundles a real PostgreSQL binary so the Supabase migrations can be
# executed and their functions exercised in CI, not merely parsed.
pgserver>=0.0.14
psycopg[binary]>=3.1
